import { Expression } from '@cucumber/cucumber-expressions';
import { Envelope } from '@cucumber/messages';
import { Suggestion } from '../suggestions/types.js';
export declare type MessagesBuilderResult = {
    suggestions: readonly Suggestion[];
    expressions: readonly Expression[];
};
/**
 * Builds MessagesBuilderResult from Cucumber Messages.
 */
export declare class MessagesBuilder {
    private readonly parameterTypeRegistry;
    private readonly expressionFactory;
    private readonly expressions;
    private stepTexts;
    processEnvelope(envelope: Envelope, errorHandler?: (err: Error) => void): void;
    build(): MessagesBuilderResult;
}
//# sourceMappingURL=MessagesBuilder.d.ts.map