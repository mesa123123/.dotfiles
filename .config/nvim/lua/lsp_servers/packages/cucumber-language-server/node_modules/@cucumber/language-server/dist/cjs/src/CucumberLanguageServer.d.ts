import { ParserAdapter } from '@cucumber/language-service';
import { Connection, ServerCapabilities, TextDocuments } from 'vscode-languageserver';
import { TextDocument } from 'vscode-languageserver-textdocument';
import { Files } from './Files.js';
declare type ServerInfo = {
    name: string;
    version: string;
};
export declare class CucumberLanguageServer {
    #private;
    private readonly connection;
    private readonly documents;
    private readonly makeFiles;
    private readonly expressionBuilder;
    private searchIndex;
    private expressionBuilderResult;
    private reindexingTimeout;
    private rootUri;
    get suggestions(): readonly Readonly<{
        label: string;
        segments: import("@cucumber/language-service").SuggestionSegments;
        matched: boolean;
    }>[];
    constructor(connection: Connection, documents: TextDocuments<TextDocument>, parserAdapter: ParserAdapter, makeFiles: (rootUri: string) => Files);
    capabilities(): ServerCapabilities;
    info(): ServerInfo;
    private sendDiagnostics;
    private scheduleReindexing;
    private getSettings;
    private reindex;
}
export {};
//# sourceMappingURL=CucumberLanguageServer.d.ts.map