"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getStepDefinitionSnippetLinks = void 0;
const vscode_languageserver_types_1 = require("vscode-languageserver-types");
function getStepDefinitionSnippetLinks(links) {
    // TODO: Find the most relevant one by looking at the words in the expression
    // If none found, use the most recently modified
    const linksByFile = {};
    for (const link of links) {
        // Insert right after the last step definition
        const targetRange = vscode_languageserver_types_1.Range.create(link.targetRange.end.line + 1, 0, link.targetRange.end.line + 1, 0);
        linksByFile[link.targetUri] = {
            targetUri: link.targetUri,
            targetRange,
            targetSelectionRange: targetRange,
        };
    }
    return Object.values(linksByFile).sort((a, b) => a.targetUri.localeCompare(b.targetUri));
}
exports.getStepDefinitionSnippetLinks = getStepDefinitionSnippetLinks;
//# sourceMappingURL=getStepDefinitionSnippetLinks.js.map